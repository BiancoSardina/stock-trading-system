# 股票池流水线可靠性修复（待生产验收）

本轮仅修复生成、发布、通知与回归测试。股票选择阈值、报价新鲜度、任务时间、
观察名单裁决、订单/信号和 paper_trader 行为不变。远端两处未知热修必须在部署前对比合并。

## 完整批次与读取协议

- `stock_pool_full.py`、`stock_pool_evening.py` 使用同一个 `pool_pipeline.py`。
- 每次在 `STOCK_DATA_DIR/pool_batches/<时间_随机ID>/` 暂存股票池和裁决包。
  上一批池仅复制用于生命周期；本轮必须完成全量目标扫描并重新生成同批次包。
- 校验 schema、当日/24小时有效期、市场状态、完整扫描、股票明细、生成时间、批次ID；
  两文件写完后计算 SHA256，写 `ready.json`，最后原子替换 `pool_batch_latest.json`。
- 发布前失败：当前指针不变；不会将部分扫描结果或新池旧包当作完成。
- 发布后上传失败：当前指向完整本地批次，文件保留可重试。完整本地数据不等于Git上传成功。
- `runtime.data_path`、池管理、技术分析、复盘、旧超跌工具和归档入口按新指针读池。
  技术分析将报告与池摘要固定在同一批次。无指针时兼容旧路径；指针损坏不静默回退旧文件。
- **启用后仓库根目录的旧 `stock_pool.json`、`decision_bundle_latest.json` 不再是最新数据。**
  不维护两份易混批的“最新”副本。外部自写脚本应通过 `runtime.data_path` 或读取批次指针访问；
  外部AI继续读取 Git `data_packages/*`，文件命名仍匹配原有 `*_*.json`。
- 启用后单独运行 `stock_pool.py` 不允许覆盖当前批次；生产请用两个流水线入口。
  `--limit` 仍是只读诊断；不可将限制样本描述成全市场。
- 未删除或取消Git跟踪已有运行数据，尤其保留 `watchlist.json` 的Git同步方式。

## 预算与可观测性

默认总预算2400秒（2026-09-22 由 900 上调：新口径 startup/v1 实测候选 1593 只、约 0.55s/只，扫描步约 880s，旧默认只剩 640s 必撞线），预留生成包120秒、上传120秒、故障通知20秒，扫描最晚在2140秒截止。
较早完成的阶段将多余时间留给后续阶段。配置项：
`PIPELINE_TIMEOUT`、`PIPELINE_BUNDLE_BUDGET`、`PIPELINE_UPLOAD_BUDGET`、`PIPELINE_NOTIFY_BUDGET`。
清理超时进程本身还有有限开销，900秒不是操作系统调度意义上的绝对墙钟上限。

`pipeline_runs/<run_id>.json` 记录各阶段、预算、耗时、错误与遗留批次锁；同名 `.log` 保存子进程输出。
`pool_batches/<batch_id>/scan_metrics.json` 记录全市场/基础过滤/行业候选/已处理/成功/排除/失败、
耗时、预计剩余时间及缓存命中。开始、每100只和完成时刷新；硬杀可能只留下最近一次进度。
失败不截断股票列表凑“成功”，也不自动启用 `--fast`。

父流程用OS锁避免午盘/收盘重叠，进程退出即释放；子任务锁隔离到独立批次。
超时终止本次子进程树（Linux进程组；Windows taskkill），不删除其他任务的锁。
扫描/打包/上传失败都走独立、限时的QQ失败提醒，并输出Hermes stdout兜底。
网络或凭据故障仍可能导致QQ提醒失败，此时运行记录明确 `failure_notification` 失败。
父进程被外部SIGKILL或机器断电不能保证即时告警；遗留 `status=running` 需要调度器核查。

## 上传与重试

归档拒绝缺文件、混批、未知市场和过期数据。只暂存本批指定的归档文件；已有其他暂存改动时拒绝提交。
重试沿用相同文件名；若上次commit成功但push失败，即使本轮无diff也会再次push。
`.upload.json` 收据区分Git推送完成与QQ通知完成，避免把通知失败误认为未推送。

当天完整批次可执行：

```sh
python stock_pool_full.py --retry-batch <batch_id>
# 收盘批次也可用 stock_pool_evening.py --retry-batch <batch_id>
```

重试不会重新扫描、不会将当前指针回退到旧批次；每次重试有单独记录，不覆盖原失败记录。
隔日批次拒绝重试发布，应生成新鲜数据。Git分叉/凭据问题仍需人工解决，不强推或自动改写历史。

## 缓存与性能边界

`daily_history/日期/代码_长度.json` 仅缓存当天获取的已完成日线；当日K线不缓存，
每次用重新取得的报价对齐。股票池与无持仓个股分析复用该缓存；已有持仓/ETF执行路径不变。
日期变化、数据损坏、日线与报价昨收不一致都会重新请求；历史缓存不能绕过实时报价日期检查。
`DAILY_HISTORY_CACHE=0` 可禁用缓存进行对照。暂不引入多线程或放松限流。

**缓存只可能改善重复运行；首轮冷缓存能否在预算内完成仍待交易日实测。**
本轮不承诺900秒内必成功，不以历史快照回放代替真实网络耗时测试。
交易日验收应记录冷/热缓存候选数量、请求错误率、各阶段耗时和最终批次/上传收据。

## 离线回归

```sh
python -X utf8 tests/run_offline.py
```

统一入口现包含启动策略和流水线测试；均使用临时数据目录并禁用网络。
覆盖扫描/打包超时保留旧批次、上传失败保留完整批次、缺失/混批拒绝、重试、预算预留、
进程树清理调用、只暂存本批文件、缓存与周末日期边界。测试不证明生产速度或策略收益。
