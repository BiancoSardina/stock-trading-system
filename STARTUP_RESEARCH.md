# 启动机会研究 startup/v1

## 本次范围

用户确认的方向：寻找“启动潜力＋合适位置”，不是预测涨停，也不是等待放量大涨之后才推荐。
仅修改 Python 候选筛选、技术分析研究和包内解释约定；不改变任务时间、不写观察名单、不改变订单执行规则。

### 两类候选

- 整理启动型：近期区间收窄、低点稳定、抛压减弱、重心趋稳。
- 超跌企稳型：过去60根完整日线高点至最近收盘回撤至少8%，且通过相同企稳结构检查。
- MA20/MA60和传统S/A等级不再是这两类候选的硬门槛。传统评分保留为背景，不冒充新机会评分。
- D级允许监测候选，禁止研究买入触发；UNKNOWN禁止买入，股票池生产入口仍拒绝异常市场数据。
- 取消研究中的本金、单笔比例、整手数量拦截。真实/模拟订单的费用、数量、资金、T+1和持仓退出检查不变。

## 可核验的默认规则（尚未做收益回测）

`startup_policy.py`集中实现无状态、无网络、无文件写入的规则。仅使用当日之前已完成的日线建立结构；
实时报价只用于触发或否决，不用当日高低点重新抬高支撑、压力或止损。

五因子：结构25、企稳20、抛压20、相对强度15、位置20，总分60为候选底线。
相对强度用同日期的最近5日超额收益及较此前5日的改善，基准缺失则不加分。
这些阈值是可审查的初始研究参数，不是从宝新能源或精达股份拟合出的胜率结论。

- 最近5日低点与此前5日相比最多下探0.25ATR；短期重心不能持续走弱。
- 最近5日振幅不超过8%，并相对前5日收窄/保持窄幅。
- 成交量收缩或下跌K线量弱于上涨K线才计抛压改善；缩量阴跌仍被结构门槛拒绝。
- 现价距结构失效位不得超过6%，距此前10日高点不得超过2%，防止涨远追入。
- 流动性依据最近20个完整交易日的`收盘价×成交股数`平均值（成交额近似值）至少2亿元；
  不再用当天未完成的成交额淘汰缩量候选。启动扫描取消5元名义价格门槛；ST/退市/主板权限过滤不变。
- 若启动扫描范围扩大造成限流/超时，需实测优化缓存与调度；现有超时退出仍不得发布部分结果。
  `--fast`保留为显式降级，会按成交额截取候选，不能声称全市场覆盖。

## 买点和预算解耦

每次只生成一条路径：现价未突破则核验支撑附近转强；已小幅突破则仅A/B市场允许核验首次突破。
突破目标必须是过去已存在的更高压力，缺失就不给目标/买入区间，不编造目标凑盈亏比。
区间上沿同时受结构、追高上限和价格盈亏比约束：

`最高入场价 <= (历史目标 + 1.5 × 失效位) / 2.5`

价格按分取整后再次保证整个区间达到未扣费价格盈亏比至少1.5；下沿高于上沿则无合格区间。
现价必须在区间内、收复开盘价/昨收/已完成日线MA5、当日未触及失效位，才标为“早期条件满足”。
日线与报价不能证实分钟级持续企稳，因此始终注明证据限制，不声称是分钟级回放或已经成交。
止损观察期仍由技术分析从原状态中只读核验，观察期内不得触发。

## 调用及兼容

- `stock_pool.py`：实际生产评分使用新机会分和两条路径，保留`strength_score`背景；
  `score_basis`和`opportunity`明确版本、证据、区间与限制，旧格式数据仍走旧兼容分配规则。
- `short_term.py`：`ANALYSIS_ONLY=1`的无持仓个股走预算无关研究，提前返回，不进入订单状态机，
  不写订单/信号；所有WATCH候选均分析，不被传统等级或前5只展示限制隐藏。
- 已有持仓与ETF仍保留原风险/执行逻辑，本轮不是修改持仓加减仓或ETF交易策略。
- `decision_bundle.py`、`tech_analysis_bundle.py`：保持v1 schema，追加研究口径约定。
- `report_contract.py`和手动/定时解释入口允许展示最多3个候选及条件，而非只展示可执行订单。
- `short_term_manual.py`与`short_term_ai.py`显式强制分析模式：它们生成研究报告与持仓风险预览，
  不再从父进程继承可写交易状态的模式；报告发送行为本身未改变。
- 外部 Codex 定时任务本身的时间、提示词和远端部署未在本次代码编辑中修改；需部署新代码后重新生成包。
  若外部任务提示词与包内新口径冲突，应另行同步，不可仅依靠旧历史数据包宣称新逻辑生效。

## 验证边界

流水线完整批次发布、预算预留、缓存与失败重试的运行说明见 `PIPELINE_RELIABILITY.md`。
新入口启用后请通过批次指针读取股票池，不再将根目录旧文件当作最新数据。

离线测试检验未来数据隔离、缺失/异常数据、持续阴跌、两条路径、风险区间、D/UNKNOWN和C级规则、
不同预算结果一致、无订单副作用、旧池生命周期及原执行回归。它们不证明选股收益或胜率。
部署前仍需不同市场时段的滚动样本回放、失败样本、滑点/费用和扫描耗时验证。
