# v3.2：交易记录驱动的入场约束更新

2026-09-03，本地更新并完成运行。基于v3.1修复版，不修改历史交易、真实持仓及既有模拟账户。尚未将这些文件部署到原Hermes主机，也未推送GitHub。

这次调整以过滤缺乏经济收益空间的交易、减少止损后的快速反复进出为目的。没有宣称已提高胜率，没有用旧历史重新标记成v3.2盈利记录。

## 实施内容

1. 选股评分保留为候选排序，新增独立入场检查。A级个股必须收复5日均线、开盘价与昨收价，随后仍需通过结构买区、费用和盈亏比检查。S级也不能绕过价格条件。
2. 支撑来自已完成日线的近5日低点与MA10。移除“支撑太远就抬高到现价97%”和“尾盘把买区重置到现价附近”。买区上沿为结构支撑的1.015倍，这是新的风险政策默认值，并非回测最优参数。
3. 目标只采用已观察的近5日高点。必须满足止损价低于入场价、目标高于入场价；没有有效目标时不制造突破追涨计划。原来的3%结构止损距离保留，持仓硬止损没有统一放宽。
4. 使用与模拟盘一致的手续费和滑点假设，按实际建议数量计算净目标利润和止损风险，要求净盈亏比至少1.5。数量不足或费用吃掉目标利润就放弃，不为摊费用加大预算。
5. 有持仓且触发止损时记录风险日期，阻止该标的买入/加仓，直到经过2个后续完整交易日。该记录是止损风险信号，不是假定成交。清空持仓不会清掉风险记忆；周末不计天数；观察期不阻挡退出。
6. 手动和定时AI报告共用解释规则，不能把程序拦截改写为条件买单，不能强制尾盘买入或自行上移买点。
7. 新信号写入`signal_log_v32.csv`，附入场止损、参考目标和净盈亏比；新模拟账户为`paper_positions_v32.json`。模拟买入不超过信号数量，并在资金不足缩小数量后重新核算盈亏比。旧v3.1及更早记录保留。
8. 正式运行将候选入场检查及拦截原因保存在`entry_reviews/`，便于将来同时研究被选中和被排除的候选；预案运行不写正式信号、状态或该目录。

## 配置与解释

- `ENTRY_MIN_NET_RR`默认1.5，允许配置但不得低于1。
- `STOP_COOLDOWN_SESSIONS`默认2，至少1个完整后续交易日；使用日线日期计数。数据缺失会延长观察期，不会提前释放。
- 费用与滑点沿用`PAPER_COMMISSION_RATE`、`PAPER_COMMISSION_MIN`、`PAPER_STAMP_RATE`、`PAPER_TRANSFER_RATE`、`PAPER_SLIPPAGE`。这些是可配置建模假设，不是从券商账户读取的实际费率。
- 新模拟账户沿用v3.1账本结构，账户文件、信号文件和策略标签隔离；仍是按报价假设成交，不能代表交易所实际成交。
- 仍以`STOCK_DATA_DIR`中的真实持仓为事实源。用户仅提供金额时不能推导成本、可卖数量，不能据此建立虚构持仓。
- 无新增全市场排名权重优化。历史样本不足以证明把A级门槛上调到某个分数可以稳定获利；新增1.5盈亏比、1.5%买区宽度和2日观察期均待前瞻验证。

## 验证结果

`python -X utf8 tests/run_offline.py`：6个套件通过，包含原32项决策检查、27项股票池检查、4组市场场景、42个故障回归、2个AI契约场景和18个新增入场场景。

同一份2026-09-03 14:47快照的36只完整分析中，固定原报价、五因子得分、市场状态和原仓库空仓输入，仅替换决策代码：建仓建议从3个变为0个。此对比验证的是规则变化，不是收益或胜率提升。

15:36的新行情运行取得47只资产和4个指数的数据，36只完整分析均未产生买入建议。涨跌家数/成交量相关接口缺项，市场状态为UNKNOWN，新增风险敞口被禁止；运行记录标为`partial_data`。当前处于盘后，也只生成预案。

## 后续胜率口径

新版本的已完成交易胜率从真实成交确认后统计，未平仓、估算价和信号不算已完成成交。固定五日信号价格表现单独统计，至少等待五个完整交易日；同时查看平均收益、亏损大小、成本和回撤，不能仅凭胜率判断改善。
