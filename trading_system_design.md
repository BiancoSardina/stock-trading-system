# A股短线交易系统 — 交易逻辑与程序设计完整文档

版本：v2.31（2026-08-05 补丁：信号状态字段 + 虚拟交易跟踪 + 策略版本号）
适用范围：ETF + 沪深主板个股短线交易
作者：Hermes Agent 为个人交易者定制

> 本文档是系统的唯一权威说明，整合交易逻辑（第一部分）与程序实现（第二部分）。
> 代码位置：`~/.hermes/scripts/`（cron 实际运行目录）；`/home/ubuntu/` 为同步副本。

---

# 第一部分 交易逻辑

## 1. 系统定位与资金概况

| 项目 | 配置 |
|------|------|
| 总资金 | ETF 5万 + 个股 5万 |
| 交易风格 | 短线波段（1-5日），盘中定时决策 |
| 决策频率 | 每日 09:25 / 11:10 / 13:30 / 14:45 + 收盘后 18:00 预案 + 18:15 复盘 + 随时手动 |
| 投递渠道 | QQ 机器人推送（微信iLink已弃用） |
| 核心原则 | 前瞻预案式（禁止马后炮）、可执行（禁止"两头堵"）、纪律优先 |

当前持仓（2026-08-10 确认，权威=positions.json）：
- 半导体设备ETF(159516) 约2.6万 @0.841（套牢，no_sell=true 禁清仓，可做T）
- 创新药指ETF(159858) 约6052元 @0.712（止损0.66/止盈0.71）
- 通信ETF(515880) 已于2026-08-07清仓 → 转入观察清单

## 2. 标的池与权限约束

| 类别 | 标的 | 说明 |
|------|------|------|
| 核心ETF | 510300沪深300 / 588000科创50 / 159732消费电子 / 518880黄金 | 等权25%组合 |
| 附加ETF | 159516半导体设备 / 159858创新药指 | 159516套牢禁清仓(no_sell) |
| 观察ETF | 512400有色 / 515220煤炭 / 512170医疗 / 513180恒生科技 / 510880红利 / 512100中证1000 / 159869游戏 / 515880通信(清仓转观察) | 出信号可建仓 |
| 观察个股 | stock_config.py 中 11 只 | 沪深主板 |
| 权限约束 | 仅沪深主板(600/601/603/605/000/001/002/003) | 用户未开通创业板(300/301)/科创板(688)/北交所 |

## 3. 技术指标体系

### 3.1 市场环境评分 Market Score（总闸门，先于个股）
四维加权 0~100，决定当日允许仓位与买卖总开关：

| 维度 | 权重 | 数据来源 | 打分要点 |
|------|:---:|---------|---------|
| ① 指数趋势 | 30 | 上证/创业板/科创50 日K | 每指数：站上MA20得10 / 破MA20守MA60得5 / 破MA60得0 |
| ② 赚钱效应 | 30 | 东财 ulist f104/105/106 + 涨停池/跌停池 | 涨跌家数15 + 涨停净家数15 |
| ③ 成交量 | 20 | 东财上证K线 f57 | 放量涨20 / 平量涨15 / 缩量涨10 / 放量跌0 / 缩量跌10（盘中按时间进度折算） |
| ④ 外部环境 | 20 | 东财 f170 | 纳指8 + 英伟达6 + 美元6 |

状态分级（写入全局 MARKET，供个股分析引用）：
- A强势(≥80) 仓位80% / B正常(65~79) 仓位60% / C防守(50~64) 仓位30% / D禁止(<50) 仓位10%
- **D级硬闸门禁买**：一切买入/加仓建议降级为观望，只保留减仓/止损
- C/D级不追涨、B级追涨仅轻仓；买入金额 × 仓位/80（A=1.0 / B=0.75 / C=0.375）

### 3.2 个股评分（五因子模型，0~100）
| 因子 | 权重 | 组成 | 子项分 |
|------|:---:|------|--------|
| ① 趋势 | 30 | 价vsMA20 / 布林中轨 / MA20 vs MA60 / MA60斜率 / 周线 | 5+1+8+8+8 |
| ② 动量 | 20 | RSI14 / MACD(DIF/DEA+零轴) / 20日涨幅 | 7+7+6 |
| ③ 资金 | 20 | 量能+方向(量比) / 换手率(东财f8) / 主力净流入占比(东财f184) | 8+6+6 |
| ④ 相对强度 | 20 | 跑赢上证(RS20) / 跑赢沪深300 / 20日绝对强度 | 8+6+6 |
| ⑤ 风险 | 10 | ATR波动率 / 20日最大回撤（波动越低分越高） | 5+5 |

- 资金因子用东财 ulist 单次批量请求（f8=换手率，**f168 是垃圾字段勿用**），失败走中性分兜底
- 等级映射：S≥80 / A 65~79 / B 50~64 / C 35~49 / D<35
- 偏空判定：评分 < 50（C/D级）触发减仓/禁买

### 3.3 趋势三要件（中期趋势确认）
1. MA60 向上（斜率>0）
2. 现价在 MA60 上方
3. MA20 在 MA60 上方
三项全满足 = 趋势成立；否则 = 趋势未确认（不能作为趋势买入依据）。

### 3.4 RS 相对强度（相对强势确认）
20日涨幅 vs 大盘（上证指数）20日涨幅，另设沪深300第二基准：
- RS>0 = 跑赢大盘 = 强；RS<0 = 跑输 = 弱
- "趋势成立 + RS居前" 才给买入预案

### 3.5 威科夫四阶段（位置判断）
吸筹（不追等突破）/ 拉升（顺势，回踩买/突破追）/ 派发（减仓，反弹卖）/ 下跌（禁买，等企稳）。

### 3.6 多空计数（8项偏多信号）
价>MA20 / MA20>MA60 / MA60斜率>0 / 周线多头 / DIF>DEA / RSI14>50 / 放量上涨 / RS20>0。
≥6项共振看多；≤2项偏空承压。

## 4. 买入逻辑

### 4.1 买入门槛（can_buy）
```
can_buy = 现价 > MA20 且 周线不空头
```
趋势向下时禁止买入/加仓，只允许减仓。市场 D 级时 can_buy 强制为 False。

### 4.2 双买点机制（核心，防"永远买不到"）
```
买点① 回踩买：回踩【动态支撑】企稳（缩量止跌）→ 买入
买点② 追涨买：放量突破【近5日高点×1.005】→ 追入（止损放突破位下方）
```
动态支撑（贴近现价，防"够不着"）：
```
dyn_sup = max(近5日最低点, MA10)         # 取较高者
若 dyn_sup < 现价 × 0.93:                 # 距现价>7% = 追不上
    dyn_sup = 现价 × 0.97                # 上移到现价附近（回踩2-3%即触发）
买入区 = [dyn_sup, min(现价, dyn_sup × 1.03)]
```

### 4.3 三段预案（每个建议品种必须提前写死）
```
① 追涨触发价：放量突破 X（=近5日高点×1.005）
② 回踩买价：  回踩 Y（=动态支撑）企稳
③ 放弃条件：  跌破 Z（=动态支撑×0.97，趋势破坏）→ 明确"不接"
```
预案触发就按预案执行，**禁止事后改口**（禁止"涨了说别追、跌了说偏空"的两头堵）。

### 4.4 买入区铁律
- 禁止把远离现价的前期低点/20日低点/起涨点当买入区——那些是"趋势破坏位=放弃位"
- 现价已涨超5%时，禁止写"等回踩前期低点"这种永远等不到的废话，必须给追涨预案

### 4.5 仓位建议
| 信号 | 建仓规模（占总资金） | 示例（5万资金） |
|------|---------------------|----------------|
| S/A级 | 2%~5% | 1000~2500元 |
| B级 | 2%（轻仓试探） | 1000元 |
| 加仓(持仓) | 8%~20%底仓比例 | 视情况 |

## 5. 卖出/止盈逻辑

### 5.1 常规卖出（sell_zone）
```
卖出区 = [近5日最高点 × 0.98, 近5日最高点]
```
- 现价进入卖出区 = 短线压力位，可分批止盈
- 放量滞涨/冲高回落 → 提前减仓；反弹到目标位（T）止盈

### 5.2 持仓减仓信号
| 信号 | 动作 |
|------|------|
| C级(35~49) | 减仓15% |
| D级(<35) | 减仓20%，破前低必须走 |
| 破前低20日低点 | 必须走 |
| 北方稀土 | 反弹至成本区放量滞涨可减1/3 |

### 5.3 四重退出系统（第五阶段，对每个持仓独立输出【📤 退出系统】区块）
数据源：持仓成本/买入日期/固定止损统一来自 `~/.hermes/scripts/positions.json`。
**唯一算法实现在 `position_manager.build_exit_plan()`**（short_term.py 委托调用，
盘后风控报告 `generate_risk_report()` 直接复用，两处输出保证一致）。
四重退出任一触发即执行，多个触发取最严格者：

**① 止损退出（价格+ATR）**
```
止损线 = max(MA20, 买入价 - 2×ATR14, 固定止损)   # 三条取最高 = 最先触发
现价 ≤ 止损线 → 无条件清仓（无容错、不商量）
```
北方稀土固定止损 37.51 为底线，不允许下移。

**② 趋势退出（防提前卖飞）**
```
适用：盈利持仓（pnl>0）且现价在 MA20 上方（上涨趋势中）
现价 > MA10 > MA20 → 健康持有，不提前卖
收盘破 MA10        → 减仓 50%
收盘破 MA20        → 清仓
```
若止损退出已触发（破止损线），趋势退出清仓不重复报。

**③ 盈利保护（锁浮盈）**
```
盈利 ≥ 10% 激活保护
保护线 = 买入后最高价 × 0.92（自买入日起 K 线最高 high）
现价 ≤ 保护线 → 卖出（清仓）
```

**④ 时间退出（资金效率）**
```
持有 ≥ 10 个交易日 且 现价 ≤ 买入价（未上涨）→ 卖出
```
- 已盈利的持仓不受时间退出限制（交给趋势退出/盈利保护）
- 套牢盘时间退出只提示"反弹减仓"，不强制清仓

**套牢盘（no_sell）特殊处理**：四重退出信号全部只作"反弹减仓/做T"提示，禁止清仓建议。

## 6. 止损逻辑

### 6.1 通用止损（stop_zone）
```
止损位 = 动态支撑 × 0.97
```
- 买入后跌破止损位 → 无条件卖出
- 追涨买入 → 止损放突破位下方（买点②的支撑位）

### 6.2 固定纪律止损（用户定死）
| 标的 | 止损线 | 规则 |
|------|--------|------|
| 北方稀土(600111) | 37.51 | 跌破无条件卖出，不商量、不拖 |
| 北方稀土 MA10 | MA10锁定线 | 收盘跌破减仓至少一半 |
| 半导体设备/通信 | 无清仓止损 | 套牢盘禁清仓（见第7节） |

### 6.3 止损原则（用户偏好）
- 区分"系统性恐慌"vs"结构性衰退"再决定是否止损（不机械止损）
- 破位先出局，即使尾盘拉回也先卖（可次日接回）

## 7. 套牢盘做T逻辑（159516 / 515880）

**规则：禁止清仓/割肉离场，只允许做T和加仓摊低成本。**

### 7.1 T+1 铁律
做T必须"先卖后买"（高抛卖老仓 → 回落接回）：接回的新仓**次日才能再卖**；禁止"先买后卖"式假T。

### 7.2 做T点位
```
做T仓位：底仓的15%（每次）
高抛位：max(近3日高点, 近5日高点) 附近（并入当日实时高点）
低吸位：min(近3日低点, 近5日低点) 附近（并入当日实时低点）
```
日内位置判断：现价在日内高位(>70%区间) → 提示高抛回落接回；低位(<30%区间) → 先低吸反弹高抛。

### 7.3 加仓规则
- 仅当 quality∈(S,A) 且 can_buy → 加仓10%（B级小仓加5%）
- 加仓点位：回踩动态支撑企稳（缩量止跌）
- C/D级"暂不加仓"→ 记录为 hold 信号（不污染买入统计）

## 8. 时段策略

| 时段 | 任务 | 策略重点 |
|------|------|---------|
| 09:25 盘前 | 短线盘前信号 | 集合竞价铁律，挂单预案为核心 |
| 10:00-11:00 | （量化收割） | **禁止买入**，量化集中收割时段 |
| 11:10 收割后 | 短线收割后信号 | 收割结束，观察捡漏，低吸为主 |
| 13:30 午后 | 短线午后信号 | 方向确认，午后预案 |
| 14:45 尾盘 | 短线尾盘信号 | 只剩15分钟：**现价直接买/不买**，禁止"回踩挂单"废话 |
| 18:00 收盘后 | 个股挂单预案 | 全市场筛选（四角色AI），为明日挂单 |
| 18:15 收盘后 | 交易复盘 | review.py 统计信号胜率，验证规则有效性 |

### 8.1 集合竞价铁律（09:25）
| 竞价情形 | 预案 |
|---------|------|
| 高开≥2%（强势高开） | 勿追开盘价！等回踩（MA5/竞价低点/昨收附近）或冲高回落企稳再进；持有可借高开做T高抛 |
| 高开0.5-2%（正常） | 可竞价挂单或开盘小幅回踩买入，给具体挂单价 |
| 平开±0.5% | 方向未定，观望，等开盘15分钟方向明确，或挂支撑位低吸 |
| 低开-2%以内 | 不抢反弹，等企稳信号（放量止跌）再进 |
| 低开<-2%（弱势） | 禁止买入抢反弹！持有者检查止损 |
| 竞价放量(>30%×5日均量)+高开 | 强势信号；竞价缩量高开 = 虚高防回落 |

### 8.2 尾盘铁律（14:45）
- 禁止"回踩挂单"类废话（时间不够）；必须给明确结论：现价直接买/不买（吃明日溢价）
- 看好就现价买，不挂单、不追尾盘急拉，14:55前完成下单

## 9. A股交易规则适配

| 规则 | 实现 |
|------|------|
| 整手交易 | ETF按100份整数倍、个股按100股整数倍（round_lot/lot_text/sell_text，sell_text 覆盖≥98%按清仓） |
| T+1 | 当日买入当日不可卖；做T必须先卖后买 |
| 涨跌停 | 涨停股不追高（量比不足/超买否决） |
| 权限 | 只分析沪深主板个股（600/601/603/605/000/001/002/003） |
| 金额换算 | 建议输出"100股(约1391元)"格式 |

## 10. 交易纪律（用户偏好沉淀，共12条）

1. **前瞻+预案式**：给条件单（回踩X买/站稳Y追/破Z止损/反弹W减），禁止马后炮
2. **禁止两头堵**：涨了必须给追涨预案或回踩动态支撑预案；跌到动态支撑按预案买，只有破位才放弃
3. **可执行性**：报告末尾必须附【今日可挂单操作汇总】表，挂单价距现价±5%内
4. **明确结论**：每个品种给明确的买/不买判断；无操作也要写"今日无操作，观望"（且带转多条件）
5. **手数整数倍**：所有建议按100股/100份整数倍
6. **买卖点位必给**：建议操作的品种必须附「建议买入点位」和「建议卖出点位」两个价位区间
7. **不机械止损**：区分系统性恐慌 vs 结构性衰退再决定止损
8. **沉没成本分离**：历史亏损与当前操作分开算账；"减仓≠清仓"不对称逻辑
9. **量化收割认知**：10:00-11:00 不买入；11:00后/14:00后是安全买窗
10. **四重退出**：止损(MA20/买价-2ATR/固定取最高)→清仓；趋势(盈利持仓破MA10减半/破MA20清仓，上涨不提前卖)；盈利保护(盈利≥10%激活，峰值回撤8%卖)；时间退出(10日未涨则卖)。触发即执行，不扛单
11. **AI角色分离（第七阶段）**：四角色递进分析——趋势分析师→风险经理→交易员→综合裁决(允许交易: YES/NO)。每角色独立调用LLM，上下文逐级传递，任一角色失败降级不阻塞
12. **交易复盘（第八阶段）**：signal_log按(代码+日期+方向+版本)去重，统计信号发出后表现（持有5日收益/最大盈/最大亏/持有天数）；按信号等级/市场状态/策略版本/行业聚合胜率。胜率<40%的等级需收紧信号
13. **虚拟交易跟踪（v2.31）**：独立虚拟账户10万，按信号自动虚拟买卖（S/A/B买入、卖出信号平仓、D级禁买忽略），与真实持仓完全隔离，用于验证策略有效性。策略每次修改递增版本号（当前 v2.31）写入信号记录，复盘按版本对比

---

# 第二部分 程序设计

## 1. 系统架构

```
┌─────────────────────────────────────────────────┐
│  cron 调度器（Hermes gateway 内置）              │
│  7个任务：0925/1110/1330/1445/1800/1815 + 手动    │
└──────────────────────┬──────────────────────────┘
                       │ no_agent 模式（stdout=报告）
┌──────────────────────▼──────────────────────────┐
│  Wrapper 脚本层（AI解读+降级链）                  │
│  short_term_ai.py / stock_picker_ai.py（四角色）  │
│  / short_term_manual.py / review.py（复盘）      │
└──────────┬───────────────────────┬──────────────┘
           │ subprocess             │ deepseek API
┌──────────▼──────────┐  ┌──────────▼──────────────┐
│  数据生成脚本        │  │  LLM 解读               │
│  short_term.py      │  │  v4-flash → chat → 兜底 │
│  stock_picker.py    │  └─────────────────────────┘
│  position_manager   │
└──────────┬──────────┘
           │ 新浪行情API + 东财数据接口
┌──────────▼──────────┐
│  数据存储            │
│  positions.json     │
│  signal_log.csv     │
│  trade_log.csv      │
│  review_report.txt  │
└─────────────────────┘
```

## 2. 文件清单

| 文件 | 职责 |
|------|------|
| short_term.py | ETF+个股技术分析引擎（数据抓取→指标→点位→文本报告→信号日志） |
| stock_picker.py | 全市场个股筛选（短期/中期候选，仅沪深主板） |
| stock_config.py | 个股观察清单配置（代码/名称/前缀/持仓金额） |
| position_manager.py | 持仓管理（positions.json读写、风控报告、**四重退出唯一算法**、资金分配） |
| short_term_ai.py | 定时任务wrapper：跑short_term.py + AI解读降级链 |
| short_term_manual.py | 手动任务wrapper：实时盘中分析（不套时段模板） |
| stock_picker_ai.py | 1800任务wrapper：跑stock_picker.py + **四角色AI解读（第七阶段）** |
| review.py | 交易复盘系统（第八阶段）：signal_log去重→等级/市场/版本/行业胜率统计 |
| paper_trader.py | 虚拟交易跟踪（v2.31）：独立虚拟账户回放信号，验证策略有效性 |
| quant_engine.py / etf_v3.py / etf_weekly.py / stock_daily.py | 旧版脚本（已弃用，保留参考） |
| positions.json | 持仓数据（含备份 positions.json.bak-*） |
| paper_positions.json | 虚拟账户数据（v2.31：现金/持仓/交易流水/去重键） |
| signal_log.csv | 信号记录（12字段，复盘用） |
| trade_log.csv | 交易日志 |
| review_report.txt | 复盘报告输出（review.py 自动生成） |

## 3. 数据源

| 接口 | 用途 |
|------|------|
| hq.sinajs.cn/list=sh600111 | 实时行情（现价/开高低/量） |
| money.finance.sina.com.cn/.../getKLineData | 日线K线（120天，scale=240，含 day 字段） |
| push2.eastmoney.com ulist | 涨跌家数(f104/105/106)、换手率(f8)、主力净流入(f184) |
| push2ex.eastmoney.com | 涨停池/跌停池（getTopicZTPool/DTPool） |
| push2his.eastmoney.com | 上证成交额K线(f57) |
| push2.eastmoney.com stock/get | 外部环境 f170（纳指/英伟达/美元） |
| deepseek API (api.deepseek.com/v1) | AI解读 |

注意：日线接口不含当日K线，操作点位计算需并入实时高低点（rt_hi/rt_lo）。

## 4. 点位计算算法（核心伪代码）

```
输入：日线K线(lows/highs/closes)、实时行情(rt)、均线(ma10/ma20/ma60)
输出：buy_zone / chase_zone / sell_zone / stop_zone

sup5 = min(近5日最低点 ∪ 当日实时最低点)
res5 = max(近5日最高点 ∪ 当日实时最高点)

# 动态支撑（防"够不着"）
dyn_sup = max(sup5, ma10)              # MA10低于现价才参与比较
if dyn_sup < 现价 × 0.93:
    dyn_sup = 现价 × 0.97              # 支撑太远→上移

buy_zone   = [dyn_sup, min(现价, dyn_sup × 1.03)]   # 回踩买入区
chase_zone = res5 × 1.005                            # 突破追涨位
sell_zone  = [res5 × 0.98, res5]                     # 短线卖出区
stop_zone  = dyn_sup × 0.97                          # 止损位
```

时段特例：
- 尾盘(14:45)：buy_zone = [现价×0.995, 现价×1.005]（现价附近直接买）
- 套牢盘：做T点位用近3日高低点 + 当日实时高低点

## 5. 评分与信号生成流程

```
analyze_item(code, name, hold, ..., pos):
  1. 抓日线K线(120天) + 实时行情
  2. 计算：MA5/10/20/60、MA60斜率、RSI14、MACD(DIF/DEA)、布林、量比、
     周线多空、RS20(相对大盘+沪深300)、ATR14、20日高低点、20日最大回撤
  3. 市场评分（全局MARKET，见3.1）→ 仓位缩放系数 pos_factor
  4. 五因子评分（见3.2）→ score(0~100) + 等级 S/A/B/C/D
  5. 多空计数(8项) + 趋势三要件 + RS强度判定
  6. 点位计算（第4节算法）
  7. 动作决策：
     - 观察清单：S/A且can_buy → 买入；B轻仓；否则带触发价观望
     - 套牢盘：做T点位 + 加仓判断（禁清仓，C/D不记买入信号）
     - 持仓：S/A加仓 / C/D减仓 / 破前低走人
  8. 四重退出系统（position_manager.build_exit_plan，见5.3）
  9. 输出格式化文本（竞价/点位/手数换算/AI解读要点）
 10. 记录信号到 SIGNAL_LOG → signal_log.csv（10字段含行业/市场评分）
```

## 6. AI解读层（模型降级链）

### 6.1 统一降级链（三个 wrapper 共用）
```
for model in [deepseek-v4-flash(超时150s), deepseek-chat(超时240s)]:
    try:
        POST /chat/completions
             {model, messages[system+user], temperature:0.7,
              max_tokens:16384, stream:false}
        content = resp.choices[0].message.content
        if content为空 → 抛异常（切下一模型）
        return content
    except (超时/429/5xx/连接错误/空内容):
        继续下一模型
全部失败 → 输出脚本原始数据 + "⚠️ AI解读暂时不可用"说明头
```
关键参数：
- max_tokens=16384：v4-flash 是推理模型，推理内容占用输出token，设小会导致正文被截断为空
- API key：config.yaml providers.deepseek.api_key（或环境变量）

### 6.2 第七阶段：四角色分离（仅 stock_picker_ai.py，1800任务）
不再用一个AI全包，四角色独立调用、上下文逐级传递：

```
角色1 趋势分析师  → 回答"趋势是否成立？"
      输出：趋势三要件(MA60斜率/价vsMA60/MA20vsMA60)、威科夫阶段、
           RS强度、周线多空 → ✅成立/❌不成立（只给结论，不给买卖建议）

角色2 风险经理    → 回答"最大风险是什么？"
      输入：趋势分析师结论
      输出：最大风险一句话、止损距离%、20日回撤%、市场环境级、
           风险等级 🔴高/🟠中/🟡低

角色3 交易员      → 回答"怎么买？多少钱？"
      输入：趋势结论 + 风险评级
      输出：三段预案（①追涨触发价②回踩买价③放弃条件）+ 目标位 +
            仓位按市场级缩放 + 【明日可挂单操作汇总】表

角色4 综合裁决    → 每只标的 "允许交易: YES/NO"
      输入：三角色全部结论
      裁决规则：趋势❌ 或 风险🔴高 或 市场D级 → NO
               趋势✅ 且 风险🟠中/🟡低 且 市场A/B/C级 → YES
      输出：市场定调 / 允许交易清单(YES/NO+理由+关键价位) / 持仓处理 /
            挂单汇总 / 风控底线
```
降级策略：任一角色调用失败 → 该角色上下文标记"（无）"继续后续；综合裁决失败 → 拼装三角色输出；全失败 → 兜底原始数据。

### 6.3 报告内嵌系统提示词（全部交易铁律）
- short_term_ai.py：铁律10条（前瞻预案/买入区动态化/三段预案/汇总表/手数/套牢盘/五因子评分口径/四重退出解读）
- short_term_manual.py：铁律12条（同上+四重退出）
- stock_picker_ai.py：铁律7条 + 四角色各自输出规范 + 市场环境闸门（D级禁买）

## 7. cron 调度集成

| 任务 | job_id | schedule | script | 模式 |
|------|--------|----------|--------|------|
| 短线盘前 0925 | 18b0fc89347a | 25 9 * * 1-5 | short_term_ai.py | no_agent |
| 短线收割后 1110 | 46b6dac0d7da | 10 11 * * 1-5 | short_term_ai.py | no_agent |
| 短线午后 1330 | e12651df2173 | 30 13 * * 1-5 | short_term_ai.py | no_agent |
| 短线尾盘 1445 | 5a5eadd08e6d | 45 14 * * 1-5 | short_term_ai.py | no_agent |
| 个股预案 1800 | 02d4e5f89b25 | 0 18 * * 1-5 | stock_picker_ai.py | no_agent |
| 交易复盘 1815 | fc8b1bd1ea4d | 15 18 * * 1-5 | review.py | no_agent |
| 手动实时分析 | d2543a517b1b | 0 9 * * 1-5(paused) | short_term_manual.py | no_agent |

机制要点：
- no_agent=True：脚本 stdout 即投递内容；空 stdout = 静默；非零退出 = 错误告警
- 投递：qqbot:4280B621120ABAD4D7E837F57EF66187
- 报告存档：~/.hermes/cron/output/<job_id>/<时间戳>.md
- 手动任务：resume → run → 完成后必须 pause（防次日自动触发）；手动run间隔 ≥30s

## 8. 交易复盘系统（第八阶段，review.py）

### 8.1 signal_log.csv 结构（12字段，v2.31）
```
时间, 代码, 名称, 操作, 价格, 信号等级, 评分, 行业, 市场评分, 市场状态, 状态, 策略版本
```
- 状态字段（v2.31 新增）：待执行（信号刚发出）/ 虚拟买入（虚拟账户已执行买入）/ 虚拟卖出 / 已忽略（D级禁买闸门）
- 策略版本字段（v2.31 新增）：每行记录产生该信号的策略版本（当前 v2.31），支持按版本对比有效性
- 旧10字段文件自动迁移（备份 signal_log.csv.bak-*），旧±10评分体系（评分<0）行不参与统计

### 8.2 统计口径
- **去重**：同一代码+同一日期+同一操作方向+同一策略版本 只保留首次（消除重复运行污染）
- **信号有效性**：对每条买入信号，取信号日之后的K线：
  - 持有N日结果 = 信号后第5根K线收盘 vs 信号价（不足5根用最后一根）
  - 最大盈利 = 信号后至今最高价 vs 信号价；最大亏损 = 最低价 vs 信号价
  - 当日信号（无后续K线）不计胜率；隔日信号用实时价算真实收益计入
- **聚合维度**：
  - 信号等级 S/A/B/C/D：笔数/胜率/平均收益/平均最大盈利/平均最大亏损
  - 市场状态 A/B/C/D × 胜率：验证"D级禁买"闸门有效性
  - 策略版本 × 胜率（v2.31）：对比不同版本策略的有效性
  - 行业 × 胜率：哪个方向信号最准（行业字段自08-05开始积累）
- **复盘结论**：样本≥3笔中胜率最高等级优先信任；胜率<40%的等级提示收紧

### 8.3 虚拟交易跟踪（v2.31 新增，paper_trader.py）
```
独立虚拟账户：初始资金 10万（与真实账户完全隔离）
执行规则（与真实纪律一致）：
  · 买入信号(S/A/B级) → 虚拟买入 当前虚拟总资产5%，单标的≤30%，现金不足跳过
  · 卖出/减仓信号 → 虚拟卖出（有虚拟持仓才执行）
  · D级市场 → 买入信号标记"已忽略"（禁买闸门），不虚拟买入
  · 信号按(代码+日期+方向)去重，多时段重复信号只执行首次
输出：
  · 虚拟总资产/总盈亏/已实现/浮动
  · 虚拟持仓明细（成本/现价/市值/浮动盈亏）
  · 虚拟胜率双口径：已平仓 + 按现价平仓
数据存储：paper_positions.json（现金/持仓/交易流水/去重键/进度）
用法：python3 paper_trader.py [--reset] [--recent]
```
review.py 每日 18:15 自动集成虚拟账户报告。

### 8.4 输出与使用
- 每日 18:15 自动运行（cron no_agent），投递QQ
- 支持 `python3 review.py --recent N` 只看最近N笔
- 完整报告存 review_report.txt

## 9. 数据存储与日志

| 文件 | 内容 | 更新时机 |
|------|------|---------|
| positions.json | 持仓（etf/stock两组：代码/成本/金额/止损/止盈/买入日期） | 交易发生时（用 position_manager.add_position） |
| signal_log.csv | 每次buy/sell信号的10字段记录 | 每次分析 |
| trade_log.csv | 成交记录 | 交易发生时 |
| review_report.txt | 复盘统计报告 | 每日18:15 |
| ~/.hermes/cron/output/ | 每任务每次运行报告 | 每次运行 |
| ~/.hermes/logs/agent.log | 运行日志（搜job_id定位） | 实时 |
| ~/.hermes/cron/executions.db | 执行状态（claimed/started/finished/error） | 每次执行 |

## 10. 故障处理与监控

| 故障 | 表现 | 处理 |
|------|------|------|
| 模型全挂 | 报告为纯脚本数据 | 降级链自动处理，无需干预 |
| LLM响应慢 | 任务耗时>10分钟 | 600s idle超时自动杀，v4-flash正常76s |
| 脚本超时 | 退出码1 + 错误说明 | short_term 300s / stock_picker 420s 上限 |
| 投递失败 | last_delivery_error非空 | 查gateway.log，确认QQ adapter状态 |
| 数据源失败 | 新浪/东财接口超时 | 脚本内try/except，单标的失败跳过，中性分兜底 |
| 手动任务误触发 | 次日09:00自动跑 | resume后run完必须pause |
| 复盘无数据 | 行业/市场状态块"暂无数据" | 新字段自08-05积累，2-4周后自动出现 |

诊断命令：
```
hermes cron list                  # 任务状态
sqlite3 ~/.hermes/cron/executions.db "SELECT * FROM executions ORDER BY id DESC LIMIT 10;"
grep <job_id> ~/.hermes/logs/agent.log    # 运行日志
```

## 11. 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-07 | 初始：etf_v3.py + agent模式定时任务（微信投递） |
| v1.1 | 2026-08-04 | 投递改QQ；prompt升级（前瞻预案、尾盘现价结论）；弃用微信iLink |
| v2.0 | 2026-08-05 | 全面重构：no_agent模式+降级链（v4-flash→chat→纯数据）；买入区动态化+追涨位（解决"永远买不到"）；三段预案+汇总表强制；1800个股任务升级；手动实时分析任务 |
| v2.1 | 2026-08-05 | 评分模型重构（第三阶段）：-10~+10 → 五因子0~100（趋势30/动量20/资金20/相对强度20/风险10）；新增换手率+主力净流入（东财f8/f184批量抓取）、沪深300第二RS基准、20日最大回撤；等级映射S≥80/A65-79/B50-64/C35-49/D<35 |
| v2.2 | 2026-08-05 | 第五阶段：四重退出系统（止损MA20/2ATR、趋势MA10减半/MA20清仓、盈利保护10%峰值回撤8%、时间退出10日）；持仓数据源统一为position_manager；套牢盘退出信号只作提示 |
| v2.3 | 2026-08-05 | 第七/八阶段：AI角色分离（趋势分析师→风险经理→交易员→综合裁决YES/NO，四角色独立LLM调用）；交易复盘系统（review.py，signal_log 10字段化，等级/市场/行业胜率统计，1815定时任务）；修复套牢盘C/D级误记买入信号 |
| v2.31 | 2026-08-05 | 补丁：①signal_log增加「状态」字段（待执行/虚拟买入/虚拟卖出/已忽略）②新增 paper_trader.py 虚拟交易跟踪（独立10万虚拟账户，按信号回放自动买卖，胜率双口径）③增加 STRATEGY_VERSION 策略版本号贯穿信号记录与复盘（按版本对比有效性）；signal_log 迁移为12字段；去重键含版本 |
