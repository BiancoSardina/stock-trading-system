# 股票池 V1.2 设计 — 评分目标修正（2026-08-07）

## 0. 问题诊断（用户提出，代码确认）

**现状 V1.1 评分目标错了：**

```
流水线：全市场5538 → 主板过滤781 → 【行业硬过滤 ≥70】→ 142只 → 五因子评分 → total=个股×0.7+行业×0.3-位置扣 → 排序进池
```

- 行业过滤**前置且是硬门槛**：行业分<70 的股票直接出局，个股再强也无评分机会
- 行业权重 30%：个股强度只占 70%
- 结果：core 池被单一强势行业垄断（今日实测 core 4只全部生物制药 86-87分，行业轮动式输出）
- 系统目标实际是**短线可执行机会**，不是行业轮动

**V1.2 目标：找全市场最强股票，行业环境"不拖后腿"即可**

```
新流水线：全市场5538 → 主板过滤781 → 【全量五因子评分】→ 行业准入(拖后腿的排除) → total=个股×0.85+行业×0.15-位置扣 → 行业配额 → 排序进池
```

---

## 1. 必改① 行业最大数量限制（配额）

**问题**：core 被单行业垄断（生物制药 4/4）
**方案**：池生成按行业配额截断

| 参数 | 值 | 说明 |
|---|---|---|
| CORE_MAX_PER_IND | 2 | core 单行业最多 2 只 |
| WATCH_MAX_PER_IND | 3 | watch 单行业最多 3 只 |

**实现**（stock_pool.py generate_pool）：
- 按 total 降序遍历 entries，填充 core/watch 时按行业计数，某行业已满则该股跳过（进不了本层，可看下一层或落选）
- 配额优先于门槛：先满足配额（多样性），再按门槛筛选——即"每行业最多2只里取最强的"

**效果**：core 至少覆盖 cap_core/2 个行业（市场C级 cap=8 → 至少4个行业），消除单一行业垄断

---

## 2. 必改② 行业权重 30% → 15%

**方案**（stock_pool.py 常量）：

```python
SCORE_W_STOCK = 0.85      # 原 0.7
SCORE_W_INDUSTRY = 0.15   # 原 0.3
```

total = stock_score×0.85 + industry_score×0.15 − 位置扣

**效果**：个股五因子主导排序，行业分只做微调（±6分以内），弱行业里的强股票不会被行业拖出局

---

## 3. 必改③ 股票先排序，行业后过滤

**流水线重排**（stock_pool.py main）：
1. 市场状态（不变）
2. 全市场 + 基础过滤（不变）→ ~781只
3. 行业映射 + 行业评分（不变，但**不再硬过滤**）
4. **全量五因子评分**（781只全评，不再先砍到142只）
5. 位置风险修正（不变）
6. 行业准入（见必改④）
7. total 计算（个股0.85+行业0.15）
8. 池生成（排序 + 行业配额）

**耗时预算**：
- 现状：102s（142只精评）
- V1.2 全量：781只 × ~0.6s ≈ 470s ≈ **8-10分钟**（K线请求加 0.05s 间隔防456限流）
- 17:30 启动 → 17:40 前完成，18:00 AI 裁决窗口充裕（30分钟）
- **降级模式** `--fast`：批量实时行情（一次请求拉全部）粗筛 top 400 → 精评，~5分钟（默认不用，超时风险才用）

**风险与对策**：
- quotes.sina.cn 限流：实测连续20次OK；781次需加间隔 0.05-0.1s，并在 score_stock 失败重试1次
- 若某日全量评分 >15 分钟：自动触发 --fast 粗筛降级（代码内判断进度）

---

## 4. 推荐④ 行业强度改为准入条件（不硬淘汰）

**方案**：行业分三档准入（industry_rank 评分逻辑不变，只改用途）

| 行业分 | 处理 |
|---|---|
| ≥55 | 正常参与，可进 core / watch |
| 40~55 | 只能进 watch（且要求个股五因子 ≥75，防止"弱行业里平庸股"混入） |
| <40 | 排除（行业环境拖后腿，个股再强也容易补跌，短线不做） |

**实现**（stock_pool.py main + generate_pool）：
```python
IND_ELIMINATE = 40    # <40 排除
IND_CORE_MIN = 55     # <55 不能进 core（只能 watch）
IND_WATCH_STRONG = 75 # 40-55 区间进 watch 需个股分≥75
```

**配套调整（stock_pool_manager.py）**：
- 淘汰线：行业分 60 → **40**（与准入一致：只有行业"拖后腿"才淘汰，行业 40-60 的旧池股票不再被误杀）
- 宽容期/入池门槛/容量不变

---

## 5. 推荐⑤ 行业覆盖率（软指标+日报）

- 池生成后统计：core/watch 的行业分布、覆盖行业数
- 日报新增区块：
  ```
  行业分布: 生物制药2 半导体2 军工1 医药1 ...（覆盖5个行业）
  覆盖率: core 5/8行业（配额保证≥4）
  ```
- **软指标，不强制达标**：市场机会少时覆盖自然低，**绝不为了凑行业数/凑数量放宽筛选**
- 覆盖率持续 <50% 时提示"市场机会集中，谨慎追高"

---

## 5.5 筛选从严（用户 2026-08-07 补充：要"好票"，容量是上限不是目标）

**核心原则：宁缺毋滥。core 只有 2-3 只甚至为空，都是正常结果；容量/配额是最高限制，不是必须填满的目标。**

在 V1.2 基础上再加 4 道从严筛选：

| 条件 | core | watch |
|---|---|---|
| 个股五因子底线 stock_score | ≥70（A级） | ≥60（B级） |
| 趋势硬条件 | 价>MA20 ✓ **且** MA20>MA60 ✓ | 价>MA20 ✓ |
| 位置硬排除 | 20日涨幅>50% 或 偏离MA20>20% → **排除**（原仅扣10分） | 同左 |
| RSI | >85 排除；70-85 扣分（不变） | 同左 |

**实现**（stock_pool.py generate_pool / main）：
```python
CORE_STOCK_MIN = 70      # core 个股五因子底线（A级）
WATCH_STOCK_MIN = 60     # watch 个股五因子底线（B级）
CORE_TREND_REQ = True    # core 需 价>MA20 且 MA20>MA60
WATCH_TREND_REQ = "ma20" # watch 需 价>MA20
POS_EXCLUDE_RISE20 = 50  # 20日涨幅>50% 排除
POS_EXCLUDE_DIST = 20    # 偏离MA20>20% 排除
```
- 位置硬排除放在 score_stock 返回后（total 计算前），命中直接不进池
- 入池门槛 total A75/B80/C85/D90 保留（双重门槛：total AND stock_score AND 趋势）

**行为预期**：
- A/B 级市场（强势）：趋势票多，core 可接近上限
- C 级市场（震荡）：core 可能只有 2-5 只（趋势成立+个股A级的票变少）
- D 级市场：core 本来就为 0
- 18:00 AI 裁决"core 前8只"自动变成"core 全部"（2-3只就裁决2-3只，宁缺毋滥）
- 日报明确显示"达标 3 只（容量上限 8）"，数量少是筛选从严，不是故障

---

## 6. 生命周期/淘汰规则汇总（V1.2 全量）

| 规则 | V1.1 | V1.2 |
|---|---|---|
| 入池门槛 | A75/B80/C85/D90（宽容-3） | 不变（total） |
| 个股底线 | 无 | **core≥70 / watch≥60（stock_score）** |
| 趋势硬条件 | 无 | **core: 价>MA20且MA20>MA60 / watch: 价>MA20** |
| 位置硬排除 | 无（只扣分） | **20日涨>50% 或 偏离>20% 排除** |
| 容量 | A12/20 B10/18 C8/16 D0/8 | 不变（**上限不填满**） |
| 淘汰-总分 | <70 | 不变 |
| 淘汰-行业 | <60 | **<40** |
| 淘汰-趋势 | 破MA60 | 不变 |
| 升级/降级 | watch≥85升core / core<80降watch | 不变（按total排序自然实现） |
| 行业配额 | 无 | **core≤2/watch≤3 每行业** |
| 行业准入 | 硬过滤≥70 | **<40排除 / 40-55仅watch(需个股≥75) / ≥55正常** |

---

## 7. 下游影响（确认无需改动）

| 模块 | 影响 |
|---|---|
| stock_pool_ai.py（18:00裁决） | 无（读 stock_pool.json core 前8） |
| review.py（池统计） | 无（读 stock_pool.json） |
| short_term.py（盘中三路合并） | 无（读 stock_pool.json） |
| industry_rank.py | 无（评分逻辑不变，只改调用方用途） |
| stock_scanner.py | 无 |

---

## 8. 验证方案

1. `python3 stock_pool.py --limit 100` 快速验证流水线不报错
2. 全量跑一次，对比 V1.1 vs V1.2：
   - core 行业分布应 ≥4 个行业（不再单行业垄断）
   - 出现"个股强+行业中等"的票（V1.1 会被过滤掉的）
   - total 排序变化（行业分影响从 ±9分 降到 ±4.5分）
3. 单测/断言：
   - 配额：core 单行业 ≤2
   - 准入：行业<40 的票不在池内；40-55 的票只在 watch 且 stock_score≥75
4. 耗时时长记录（目标 <12 分钟）
5. 18:00 AI 裁决端到端跑一次（禁发QQ，monkeypatch 验证 watchlist 写入）

---

## 9. 参数总表（一份常量集中管理）

```python
# stock_pool.py
SCORE_W_STOCK = 0.85
SCORE_W_INDUSTRY = 0.15
IND_ELIMINATE = 40
IND_CORE_MIN = 55
IND_WATCH_STRONG = 75
CORE_MAX_PER_IND = 2
WATCH_MAX_PER_IND = 3
KLINE_SLEEP = 0.05      # K线请求间隔（防限流）
FAST_PRESCREEN = 400    # --fast 模式粗筛数量

# stock_pool_manager.py
POOL_EVICT_INDUSTRY = 60 → 40
```

---

## 10. 待确认决策点

1. **全量精评（781只 ~10分钟）vs --fast 粗筛（400只 ~5分钟）作为默认**——建议默认全量（真实"全市场最强"），fast 做降级
2. 行业配额 core=2/watch=3 是否合适（市场C级 cap 8/16 → 至少4/5个行业）
3. 行业准入线 40/55 两档是否合适（<40 排除 / 40-55 仅watch）
4. 淘汰行业线 60→40 的放宽是否接受
