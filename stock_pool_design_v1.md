# 股票池筛选模块 V1.0 设计方案（实测校准版）

> 状态：**设计定稿，待开发**（2026-08-07 数据源实测完成）
> 数据源实测日期：2026-08-07（盘中/盘后验证）
> 权限硬条件：只做沪深主板（600/601/603/605/000/001/002/003），剔除创业板(300/301)/科创板(688/689)/北交所——用户未开通权限

---

## 1. 目标与职责边界

每天收盘后：全市场股票 → 基础过滤 → 行业强度筛选 → 五因子评分 → 综合排序 → 输出 20~30 只股票池（stock_pool.json），次日进入 short_term.py 监控。

**V1 只解决"买什么股票"**。不负责：买卖点 / 仓位 / 止盈止损 / 新闻分析 / AI判断（这些继续由现有 v2.3 系统负责）。

---

## 2. 系统流程（调整后）

```
每日 17:30  单脚本串行（stock_pool.py 内部分步）
  ① 全市场拉取（新浪 Market_Center，~26s）
  ② 基础过滤（主板白名单 + ST/上市/流动性/股价/跌停）
  ③ 行业映射（本地缓存 industry_map.json，增量更新）
  ④ 行业强度评分（新浪当日聚合为主 + 东财周期字段缓存为辅）
  ⑤ 五因子评分（过滤后 ~500只 拉K线，2-3min）
  ⑥ 综合排序（个股×0.7 + 行业×0.3）
  ⑦ 生成 stock_pool.json（core 10 + watch 20）

每日 18:00  stock_picker_ai.py 读 stock_pool.json
  ① core_pool 前 5~8 只 → 四角色AI裁决 YES/NO → watchlist.json（重点监测）
  ② watch_pool 20只 不裁决，直接进次日监控名单

次日盘中   short_term.py 三路合并监控：
  stock_pool(core全量+watch简略) + stock_config.py 固定自选 + watchlist.json 重点监测
```

**为什么 17:30 单脚本而非 3 个 cron**：全流程总耗时 4~6 分钟，单脚本串行失败好处理（一步失败整体重跑）；拆 3 个 cron 要传递中间文件状态，且每段都要独立处理限流重试，复杂度不成比例。

---

## 3. 数据源实测结论（2026-08-07，不可跳过）

### 3.1 可用源（主依赖）

| 用途 | 接口 | 实测表现 |
|------|------|---------|
| 全市场列表 | 新浪 `Market_Center.getHQNodeData?node=hs_a` | ✅ 5538只/26s，稳定。字段：symbol/code/name/trade/changepercent/amount(成交额)/turnoverratio(换手)/mktcap/nmc(市值) |
| 个股日K线 | 新浪 `quotes.sina.cn getKLineData` | ✅ 系统在用，稳定。datalen≥70（MA60需要） |
| 个股实时 | 新浪 `hq.sinajs.cn` | ✅ 系统在用 |
| 行业板块列表 | 新浪 `newSinaHy.php` | ✅ 返回 JS 变量，约80个新浪行业：行业代码/名称/成分数/当日涨幅/成交额/领涨股 |
| 行业成分股 | 新浪 `Market_Center.getHQNodeData?node=hangye_XX` | ✅ 行业代码需从 newSinaHy 解析，逐行业拉成分（每行业1次请求） |
| 行业板块行情(辅) | 东财 `clist?fs=m:90+t:2` | ⚠️ 496板块可取（当日f3/成交额f6/疑似f109=5日/f110=20日涨幅），**限流严重** |

### 3.2 不可用/受限源（必须绕过）

| 接口 | 问题 |
|------|------|
| 东财 `push2his`（板块K线历史） | ❌ **确认被封**（"Remote end closed"，技能旧记录复现）。行业指数 MA20/MA60 无法直接计算 |
| 东财 `clist` 个股全量分页 | ❌ **限流极狠**：单次 pz=5 可通，连续分页请求 4~5 次后直接断开且长时间不恢复。只能慢速(≥10s间隔)+重试+缓存当辅助 |

### 3.3 过滤规模实测（盘中半天数据，收盘后约×1.5~2）

```
全市场 5538 → 沪深主板 3193 → 基础过滤(价≥5/额≥5亿/非跌停) 315（收盘后预计 500~600）
主板成交额分布：中位0.6亿 / p75=1.8亿 / p90=5.1亿
```

**结论：5亿门槛下主板池约 500~600 只（非方案原估的1500），评分负担更小。**

---

## 4. 分层设计

### 4.1 第一层：基础过滤（stock_scanner 逻辑）

1. **权限白名单（硬条件）**：代码前缀 600/601/603/605/000/001/002/003，其余全删
2. **状态**：名称含 ST/*ST/退 删除；停牌删除（新浪 trade=0 或 ticktime 异常）
3. **上市时间**：上市 ≥120 交易日（新浪无直接字段，用 mktcap 或额外接口；V1 可用"新股上市日期"接口或省略，标注为可选）
4. **流动性**：20日平均成交额 ≥5亿（V1 用当日成交额近似，收盘后跑数据完整）
5. **股价**：≥5元（trade）
6. **涨跌异常**：当天跌停删除（changepercent ≤ -9.9）

### 4.2 第二层：行业强度（industry_rank 逻辑，双轨实现）

```
行业评分 = 行业涨幅40 + 行业成交额30 + 行业趋势30
```

**数据实现（因 push2his 被封调整）：**

- **行业涨幅（40分）**：
  - 5日/20日涨幅：优先用东财板块周期字段（f109≈5日/f110≈20日，每天 17:30 慢速拉一次 496 板块写入本地缓存 industry_rank_cache.json，间隔≥10s/请求，失败重试3次）
  - 东财不可用 → 降级：新浪当日成分股平均涨幅做相对排名（5日/20日缺失，分数按当日涨幅排名折算）
  - 评分档：排名前10%=40 / 前30%=30 / 中间=20 / 后=0
- **行业成交额（30分）**：当日行业成分股成交额总和排名（新浪当日数据稳定可得）；成交额较昨日增加→高分（需缓存昨日值，V1 可用绝对排名替代）
- **行业趋势（30分）**：⚠️ "指数>MA20>MA60" 无法直接算（无板块K线）→ 改为近似：20日涨幅>0（f110缓存，方向分）+ 当日成分股平均涨幅>0。V1 接受精度损失，文档标注升级路径（换 akshare 可拿申万行业指数K线）

**保留规则**：industry_score ≥70 才进入下一层。

### 4.3 第三层：五因子评分（复用现有 short_term.py 模型，不重新设计）

```
趋势30（价>MA20 / MA20>MA60 / MA60向上）
动量20（RSI14 / MACD / 20日涨幅）
资金20（成交量 / 换手率 / 主力净流入）
相对强度20（RS20 / 跑赢上证 / 跑赢沪深300）
风险10（ATR / 20日最大回撤）
等级：S≥80 / A65-79 / B50-64 / C35-49 / D<35
```

- 数据：新浪日K线（500只×0.3s≈2-3min，datalen=70）
- 主力净流入字段新浪没有 → 用换手率+量比近似，或东财 ulist 批量（f184，慢速+缓存）；V1 可先只用换手率

### 4.4 综合排序与分级

```
最终评分 = 个股五因子 × 0.7 + 行业强度 × 0.3

core_pool  10只   最终评分 ≥80（对齐现有S级；原方案85在现有模型=超S级，易空池，已校准）
watch_pool 20只   65 ≤ 评分 <80
<65 删除
```

> 阈值校准依据：现有模型 S≥80 即最高级。85 分门槛会导致常空池。先跑通后按实际分布微调。

---

## 5. 数据文件设计

### 5.1 stock_pool.json（每日输出，唯一权威池）

```json
{
  "date": "2026-08-07",
  "market_status": "B",
  "generated_at": "2026-08-07 17:42:00",
  "core_pool": [
    {
      "code": "600111",
      "name": "北方稀土",
      "industry": "稀土",
      "industry_score": 85,
      "stock_score": 82,
      "total_score": 83,
      "level": "A",
      "reason": ["行业强度排名前30%", "趋势成立", "RS20强"]
    }
  ],
  "watch_pool": []
}
```

### 5.2 industry_map.json（行业映射缓存，一次性全量+每日增量）

```json
{
  "updated": "2026-08-07",
  "industries": {
    "半导体": {"sina_node": "hangye_XX", "em_bk": "BK1036", "stocks": ["688693","300672","600206"]}
  }
}
```

### 5.3 industry_rank_cache.json（东财板块周期字段缓存，每日17:30慢速更新）

```json
{
  "updated": "2026-08-07",
  "boards": {"BK1036": {"name": "半导体", "chg5": 3.2, "chg20": -14.5, "amount": 1.93e11}}
}
```

---

## 6. 与现有系统衔接

| 现有组件 | 改动 |
|---------|------|
| short_term.py | STOCKS 来源改为三路合并：stock_pool.json(core全量+watch简略) + stock_config.py(固定自选) + watchlist.json(重点监测)。**stock_config 保留**（用户自选票不丢失）。date 校验：stock_pool.date 非最近交易日 → 回退只用 stock_config+watchlist 并提示 |
| stock_picker_ai.py | 18:00 读取 stock_pool.json 的 core_pool，只对前 5~8 只做四角色 YES/NO 裁决（控制 4-5min 耗时）→ watchlist.json；watch_pool 不裁决 |
| watchlist.py | 不变（次日重点监测机制照旧） |
| ensure_stock_coverage | 适配新池：core 10只全量 + watch 20只简略（一行信号小结），报告长度控制在 QQ 2~3 段（3500字节/段）内 |
| 手续费/权限铁律 | 全量继承：单笔≥3000元、100股整数倍、只做主板 |

---

## 7. Cron 设计（V1）

| 时间 | 任务 | 说明 |
|------|------|------|
| 17:30 | stock_pool.py | 全流程串行（4-6min），产出 stock_pool.json / industry_rank_cache.json |
| 18:00 | stock_picker_ai.py | 读 core_pool 前5-8只 AI裁决 → watchlist（现有任务，小改） |
| 18:15 | review.py | 不变 |
| 18:20 | paper_trader.py | 不变 |
| 手动 | stock_pool.py --scan-only | 盘中随时手动跑底部/强度扫描预览 |

新增文件：stock_pool.py（主程序+评分）、industry_rank.py（行业强度）、stock_pool_manager.py（池生成/缓存管理）、stock_scanner.py（基础过滤）。V1 建议 stock_pool.py 一个入口串行调用后三者（保留独立模块便于单测）。

---

## 8. 开发注意事项（实测踩坑清单）

1. **东财限流铁律**：所有东财请求间隔 ≥10s，失败退避（6s/12s/24s），连续失败 3 次放弃该源并降级；**绝不允许分页快速循环**
2. **push2his 不要碰**（确认封禁），行业趋势用 f110 缓存近似
3. 新浪 Market_Center 分页：num=100/页，约 56 页，间隔 0.2s 即可，稳定
4. 行业映射首次全量建表约 80 行业 × 1 请求 = 80 请求（新浪无限制，间隔 0.3s ≈ 30s）；每日增量只更新领涨行业成分
5. K线并发：新浪 K线逐只 0.2-0.5s，500 只 ≈ 2-4min；可加 3-4 线程并发（注意新浪限流边界，先测）
6. 收盘后跑（17:30 数据完整）；盘中手动跑时成交额是半天值，过滤结果偏严（提示即可）
7. 测试防污染：stock_pool 不写 signal_log（无此逻辑）；但**验证时别真跑 18:00 的 stock_picker_ai 全流程**（会发 QQ），用 --dry-run
8. date 校验必须做：周末/脚本失败时 stock_pool.json 过期，short_term.py 不能拿旧池子分析

---

## 9. V1 完成标准

- [ ] 自动获取全市场股票（新浪源，26s）
- [ ] 自动过滤垃圾股票（主板白名单+ST/流动性/股价/跌停）
- [ ] 自动计算行业强度（当日聚合主源 + 东财周期缓存辅源，带降级）
- [ ] 自动计算五因子评分（复用现有模型）
- [ ] 自动生成 stock_pool.json（core 10 + watch 20）
- [ ] short_term.py 三路合并监控（core全量+watch简略+自选+重点监测）
- [ ] 18:00 AI 只裁决 core 前 5-8 只
- [ ] 全链路 17:30 起 10 分钟内完成，失败有降级有提示

---

## 10. 升级路径（V1 后）

- akshare 安装后：行业指数K线（申万/东财板块）→ 行业趋势要件完整实现（指数>MA20>MA60）
- 主力净流入字段接入（东财 ulist f184 缓存）
- 上市日期接口 → 120交易日过滤精确化
- 池子数量/阈值按 2 周实盘分布调优

---

# V1.1 增强设计（2026-08-07 用户修改计划，分析后定稿）

> 在 V1 基础上叠加 6 项修改。V1 数据源结论全部沿用（新浪主源/东财辅助+缓存/权限白名单）。

## V1.1 修改总览

| # | 修改 | 优先级 | 可行性 | 核心依赖 |
|---|------|:------:|:------:|---------|
| 一 | 市场状态控制（池子总开关） | ⭐⭐⭐⭐⭐ | ✅ 高（复用现有） | short_term.market_score() 已生产在用 |
| 二 | 涨幅/偏离/RSI 位置风险修正 | ⭐⭐⭐⭐⭐ | ✅ 高（纯计算） | 现有 RSI14/MA20/20日涨幅数据 |
| 三 | 行业赚钱效应 | ⭐⭐⭐⭐ | ✅ 中（需行业成分表） | industry_map.json + 新浪当日涨跌 |
| 四 | 成交额 2亿动态 + 流动性评分 | ⭐⭐⭐ | ✅ 高 | 新浪 amount 字段 |
| 五 | 股票池生命周期 | ⭐⭐⭐ | ✅ 高（纯逻辑） | stock_pool.json 字段扩展 |
| 六 | 复盘结构化字段 | ⭐⭐⭐ | ✅ 高 | stock_pool.json schema 扩展 + review.py 扩展 |

## 修改一：市场状态控制

**实现**：直接复用 `short_term.market_score()`（已生产运行：08-06 D级47分 / 08-07 C级57分均有输出）。返回 `{score, state}`：A≥80 / B65-79 / C50-64 / D<50。

**⚠️ 已知精度损失**：market_score 的量能因子(20分)依赖 push2his（已确认被封）→ 自动降级为"量能缺失(中性10分)"。V1.1 顺手升级：量能改用新浪 `getKLineData?symbol=sh000001` 的 volume 字段（上证指数日成交额，新浪稳定），恢复量能 20 分精度。若来不及，沿用降级版（评分偏中性，不影响等级边界太多）。

**池子规模联动（总开关，17:30 生成时按当日 market_status 决定，2026-08-07 用户定稿数字）**：

| 市场状态 | core_pool | watch_pool | 合计 | 最低入池 total_score | 附加规则 |
|:--------:|:---------:|:----------:|:----:|:--------------------:|---------|
| A ≥80 | 12 | 20 | 32 | 75 | 正常生成 |
| B 65-79 | 10 | 18 | 28 | 80 | 正常生成 |
| C 50-64 | 8 | 16 | 24 | 85 | 只留趋势强+RS强+行业强（排序优先） |
| D <50 | 0 | 8 | 8 | 90 | 极强趋势观察；**不产生买入信号**（与现有 D 级硬闸门禁买天然一致，无需额外开关） |

**与现有系统关系**：D 级禁买闸门已存在于 short_term.py（信号标记"已忽略(D级禁买闸门)"）。D 市场只出 watch 8 只（无 core），全部只观察不出买点，零额外改动。

## 修改二：位置风险修正

**计算公式（在 total 层面扣分，不进五因子）**：

```
total_score = 五因子×0.7 + 行业×0.3 - 位置风险扣分

rise20 = (现价 - 20日前收盘) / 20日前收盘
distance_ma20 = (现价 - MA20) / MA20
rsi = RSI14

扣分规则：
  rise20 30%-50% → 扣5；>50% → 扣10
  distance_ma20 10%-20% → 扣5；>20% → 扣10
  RSI14 70-80 → 扣5；>80 → 扣10
  （三项独立，可叠加，最多扣30）
```

**与五因子现有因子的关系（防双重惩罚）**：
- 五因子动量因子里 RSI 健康区(40-70)加分照旧——衡量**动能**
- 位置修正 RSI>70 扣分——衡量**追高风险**
- 两者维度不同，不冲突；reason 数组追加"20日涨幅35%超30%扣5分"等明细供复盘

**数据**：全部来自新浪日K线（rise20/distance_ma20/rsi14），纯计算，无新接口。

## 修改三：行业赚钱效应

**新行业评分模型（总分100）**：

| 因子 | 分值 | 实现 |
|------|:----:|------|
| 行业涨幅 | 30 | 5日+20日涨幅排名：前10%=30 / 前30%=25 / 中间=15 / 弱=0（东财板块 f109/f110 缓存，失败降级当日聚合） |
| 行业成交额 | 20 | 成分股成交额总和排名（新浪当日）；较昨日增加→高分（V1.1 用绝对排名+昨日缓存） |
| 行业趋势 | 30 | 三要件近似（见下） |
| 行业赚钱效应 | 20 | 行业内上涨股数/总股数：≥80%=20 / 60-80%=15 / 40-60%=10 / <40%=0（industry_map 成分 + 新浪当日 changepercent） |

**行业趋势三要件近似**（push2his 被封，无法算行业指数 MA20/MA60）：
- "指数>MA20" ≈ 5日涨幅>0（f109 缓存）
- "MA20>MA60" ≈ 20日涨幅>0（f110 缓存）
- "MA60向上" ≈ 5日与20日涨幅均>0 双重确认
- 全满足=30 / 2项=20 / 1项=10 / 0项=0；**标注精度局限，akshare 后替换**

**赚钱效应注意**：按全成分（含创业板/科创板）计算行业强弱——行业本身强弱不受用户权限限制；入池时才过滤主板白名单。

## 修改四：成交额动态化

- 基础过滤门槛：20日平均成交额 **≥2亿**（原5亿）
  - 实测影响：主板池由 ~500-600 只升至 **~1200-1500 只**（收盘后估算）
  - **性能对策**：先行业过滤（industry_score≥70）再五因子评分，评分阶段压到 400-600 只（K线 2-4min）；若仍超，按成交额降序取前 800 只评分（高流动性优先）
- **流动性评分并入资金因子**（五因子资金20分的子项）：20日成交额 >10亿=满分 / 5-10亿=80% / 2-5亿=60% / <2亿=淘汰（基础过滤已挡）

## 修改五：股票池生命周期

**字段**：每只股票 `first_seen`（首次入池日）、`days_in_pool`（连续在池天数）。

**规则（每日 17:30 生成时执行）**：
- 昨天池中今天仍在 → days_in_pool+1，first_seen 保留
- 新入池 → first_seen=今日，days_in_pool=1
- **默认保留 3 个交易日**（days_in_pool ≤3 时评分略宽松：入池分 threshold -3 分宽容；>3 严格按门槛）
- **淘汰（任一满足即移出）**：total_score<70 / 现价<MA60（趋势破坏）/ industry_score<60（行业转弱）
- **升级**：watch 中 total_score≥85 → 升 core
- **降级**：core 中 total_score<80 → 降 watch
- **与 watchlist.json 的关系**：并存不冲突——stock_pool 是候选池（生命周期管理），watchlist 是 AI 裁决的重点监测名单（次日未买入自动移出）。生命周期作用于候选池，watchlist 机制不动

## 修改六：复盘结构化字段

stock_pool.json 每只股票增加：

```json
{
  "factor": {"trend": 28, "momentum": 17, "capital": 18, "rs": 19, "risk": 8},
  "position": {"rise20": 18.0, "distance_ma20": 5.0, "rsi14": 62.0},
  "trend": {"above_ma20": true, "above_ma60": true, "ma20_gt_ma60": true}
}
```

**review.py 扩展（新增统计维度，不动现有 signal_log 口径）**：
- 池内股票次日/3日表现（入池后第1、3个交易日涨跌幅）
- 按行业聚合胜率（哪些行业有效）
- 按因子聚合（趋势强/RS强/行业强 哪类条件命中率高）
- 按市场状态聚合（A/B/C/D 市场出池的股票表现，验证市场状态控制有效性）
- 淘汰条件命中统计（total<70/破MA60/行业<60 哪条淘汰得对）

## V1.1 最终评分模型

```
total_score = 五因子(0~100) × 0.7 + 行业评分(0~100) × 0.3 - 位置风险扣分(0~30)
等级：S≥80 / A65-79 / B50-64 / C35-49 / D<35
入池门槛随市场状态浮动（A:75 / B:80 / C:85 / D:90）
```

## 更新后的 stock_pool.json（V1.1 完整 schema）

```json
{
  "date": "2026-08-07",
  "market_status": "B",
  "market_score": 72,
  "generated_at": "2026-08-07 17:42:00",
  "core_pool": [
    {
      "code": "600111",
      "name": "北方稀土",
      "industry": "稀土",
      "industry_score": 85,
      "stock_score": 82,
      "total_score": 83,
      "level": "A",
      "factor": {"trend": 28, "momentum": 17, "capital": 18, "rs": 19, "risk": 8},
      "position": {"rise20": 18.0, "distance_ma20": 5.0, "rsi14": 62.0},
      "trend": {"above_ma20": true, "above_ma60": true, "ma20_gt_ma60": true},
      "first_seen": "2026-08-07",
      "days_in_pool": 1,
      "reason": ["行业强度排名前30%", "趋势成立", "RS20领先"]
    }
  ],
  "watch_pool": []
}
```

## V1.1 与现有系统的冲突点清单（开发时逐项核对）

1. **市场评分量能因子**：push2his 被封 → 已自动降级中性分；V1.1 优先换新浪量能（小改 _volume_score，注意别动生产 short_term.py 其他逻辑，改完回归验证市场评分输出）
2. **池子规模 vs 报告长度**：A 市场 12+20=32 只，仍超 QQ 2-3 段限制 → **盘中报告策略**：core 全量详细 + watch 仅"代码 名称 评分 方向"一行（按 total 降序最多 15 行），D 市场 watch 8 只只观察不出买点
3. **评分阈值三处口径**：入池门槛(75-90) / 等级(S≥80) / 淘汰(<70)——互不冲突但别混淆，代码里用常量+注释
4. **位置扣分与五因子风险因子**：波动风险(ATR/回撤)在五因子内，追高风险(rise20/偏离/RSI)在 total 层扣，文档注明防二次开发时重复
5. **生命周期宽容期**（days≤3 门槛-3）只作用于入池判断，不作用于淘汰判断（淘汰按严格值）

## V1.1 完成标准

- [x] 市场状态控制：复用 market_score，池子规模随 A/B/C/D 联动（12/20、10/18、8/16、0/8）
- [x] 动态入池门槛：A75/B80/C85/D90
- [x] 位置风险修正：rise20/偏离MA20/RSI 三档扣分，reason 带明细
- [x] 行业赚钱效应：成分股上涨比例评分入行业分
- [x] 成交额 2亿 动态门槛 + 流动性评分入资金因子
- [x] 股票池生命周期：first_seen/days_in_pool/保留3日/淘汰/升级降级
- [x] 复盘字段：factor/position/trend 结构化输出
- [ ] review.py 新增池表现统计（行业/因子/市场状态聚合）——待开发
- [x] 全链路 12 分钟内完成（实测 102s，142只评分零失败）

## 开发记录（2026-08-07 完成）

- 新增文件：`stock_scanner.py`（全市场+基础过滤）、`industry_rank.py`（行业映射+评分）、
  `stock_pool_manager.py`（读写+生命周期）、`stock_pool.py`（主程序）
- 实测：全市场5538→主板过滤781→行业过滤142→五因子评分142只，总耗时102s
- 首次运行结果（C级63分）：core 3只=九洲药业87.6/众生药业86.2/津药药业86.2（生物制药，行业96分）
- 数据源关键决策：**K线一律用 quotes.sina.cn 端点**（money.finance 端点连续请求触发 HTTP 456 限流，
  实测 quotes.sina.cn 连续20次无456）；market_score() 返回 lines、dict 在全局 MARKET（stock_pool 读 MARKET）
- 行业 5日/20日涨幅 = 行业代表股（成分成交额前3）K线聚合（push2his 被封的稳定替代）
- 待接入：18:00 stock_picker_ai.py 读 core_pool 前5-8只 AI 裁决；short_term.py 三路合并监控；
  review.py 池表现统计
