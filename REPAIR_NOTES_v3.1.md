# v3.1 本地修复说明

日期：2026-09-03。基于公开仓库 main 的 `f150aacf6e3e5c379d2bcec44abc30e11be8388b` 源码快照。

本次修改了源码及测试，没有运行生产任务、调用收费模型、发送QQ消息或修改历史持仓与交易记录。尚未推送到 GitHub，也未部署到 Hermes 主机。

## 审计问题对应修复

| 编号 | 修复结果 |
|---|---|
| F01 买点闸门被覆盖 | 保留形态拦截，并把闸门结果传给最终状态机；预算不足一手也禁止产生买入动作。 |
| F02 HOLD仍写卖出 | 信号、行动清单与尾盘结论消费同一最终动作；输出可执行数量；T+1拦截后删除残留的强制卖出文案。 |
| F03 盈利保护漏触发 | 按已达到的持仓峰值判断激活；接入已记录峰值；新持仓周期不继承旧周期峰值，买入当天整日最高价不当作买后最高价。 |
| F04 人为回补空间 | 删除保底目标价，使用最近的可观测支撑；达不到收益要求就没有有效回补路径。此空间仍是价格结构参考，不保证成交。 |
| F05 跨日回补丢失 | 保留未完成卖价、卖量、回补数量和提醒，仅重置可重置的日内状态。 |
| F06 部分成交清空 | 按剩余数量推进状态；低吸半仓目标持续跟踪；超量、负数、非整数、错误状态及T+1违规成交拒绝入账。成交事实与T状态在一个文件内原子保存。 |
| F07 多笔持仓覆盖 | 新增数量加权聚合视图，保留原批次计算可卖数量；监控覆盖全部真实持仓，跨列表去重，移除固定金额回退。 |
| F08 模拟盘规则失真 | 新建独立v3.1账户，区分建仓/加仓/减仓/清仓；批次T+1、部分减仓、费用和滑点；按入场批次归属收益；成交和去重回执一起保存，不再回写信号CSV。 |
| F09 AI缺价格证据 | 股票池增加现价、均线、ATR、支撑、压力、止损参考与行情时间，全部送入裁决。 |
| F10 AI无稳定契约 | 使用严格JSON裁决，核对候选白名单、完整覆盖、明确YES/NO与输入价位。有效NO不重跑，不自动转条件监测；已在名单的YES也不重跑。修正风险提示词的负号比较。 |
| F11 MACD错误 | 共用EMA12−EMA26、DIF的EMA9及柱线实现；零值正常处理；周线均线改用周收盘序列；横盘RSI返回50。 |
| F12 数据异常当正常 | 持仓缺失/损坏、决策状态损坏均停止决策；市场缺项返回UNKNOWN并阻止新增买入；报价校验日期和时效；日K与当前报价对齐并核对前收盘价基准。 |

其他已处理事项：行业周期排序、同日股票池年龄重复增加、首板次日昨收基准、趋势退出收盘确认、午休成交量折算、缺报价时拒绝输出不完整资产合计、资金常量口径统一、原子写和进程锁、HOLD_ONLY预案不推进决策和做T状态、流程总时限、股票池分页完整性、QQ分段回执及公开快照同步范围。

## 文件与配置变化

核心交易链路使用环境变量 `STOCK_DATA_DIR` 指定运行数据目录；未设置时使用源码所在目录。部署时应指向现有真实运行目录，并确认其中的 `positions.json` 是权威记录。缺失文件不再自动视为空仓；新空账户须显式创建 `{"etf": [], "stock": []}`。

- 新信号写入 `signal_log_v31.csv`；模拟盘和五日信号复盘默认只读此文件。
- 新模拟账户为 `paper_positions_v31.json`，初始资金20万元。旧 `signal_log.csv` 和 `paper_positions.json` 不迁移、不重算、不改写。
- 新T成交保存在 `t_state.json` 的 `__fills__` 中，与相应状态共同提交；旧 `t_trade_log.csv` 保留，T复盘合并读取。该统计明确标为成交毛收益，部分回补不应解释为完整交易轮次。
- `delivery_receipts.json` 保存报告内容和逐段投递状态。`sent`不重发，`failed`可仅补发该段，`sending`表示结果不确定，须人工核对，系统不会自动整份重发。
- QQ目标用户须配置 `QQ_TARGET_OPENID`，已移除源码中的个人默认收件人。测试环境设置 `QQ_SEND_DISABLE=1`。
- `ANALYSIS_ONLY=1` 或 `HOLD_ONLY=1` 不生成正式信号、不推进决策/做T状态。盘后运行默认按预案处理。
- 模拟费用和滑点可通过 `PAPER_SLIPPAGE`、`PAPER_COMMISSION_RATE`、`PAPER_COMMISSION_MIN`、`PAPER_STAMP_RATE`、`PAPER_TRANSFER_RATE` 配置；默认值为代码中的建模假设，部署时须按实际产品和账户核对。
- 串联流程 `PIPELINE_TIMEOUT` 默认900秒。股票池缺任何目标报价或指数基准、分页未完整取得时停止发布；`--limit`测试结果不覆盖正式池。AI仅处理同日、生成未超过30分钟且完整性校验通过的池。
- 自动公开财务快照需要在部署主机明确设置 `PUBLISH_PRIVATE_SNAPSHOTS=1`；提交范围仅限`cron_data/`，暂存区已有其他改动时停止。`.gitignore`只防止新文件误提交，不会删除已经公开的Git历史。

## 验证

在Python 3.12下执行：

```text
python -X utf8 tests/run_offline.py
```

测试运行器复制源码到临时目录，创建空测试持仓，屏蔽网络和QQ，不读取生产行情与实际账户。

- 既有决策状态机：32项检查通过，T+1测试改为真实批次，时间按正向推进。
- 既有股票池：27项检查通过。
- 市场A/B/C/D：4组场景通过，违规现在会使测试失败。
- 新增故障回归：42个场景通过。
- AI有效裁决、既有YES及过期池：另2个场景通过。

已增加GitHub Actions离线测试配置；本次只在本地Windows运行过，远程CI未触发。

## 仍需单独验收的边界

这次修复可复现的决策、数量和记录错误，不构成策略盈利验证。以下不能通过离线单元测试证明：

1. 实时供应商字段、复权方式、交易所节假日及不同ETF的交易制度，需要用部署环境的数据做契约验收。目前采用原系统保守的T+1口径、会话时间和数据新鲜度检查，没有宣称已经接入完整交易所日历。
2. 模拟成交使用“信号报价±设定滑点”，不模拟盘口排队、涨跌停无法成交、停牌及真实委托回报；五日复盘是固定窗口信号毛收益，不能称真实交易收益。已废止不足五日也算完成及用现价替代缺失历史的口径。
3. T成交登记仍与真实持仓录入分开；实际成交后必须核对并更新`positions.json`批次，未接入券商成交自动对账。历史损坏行与旧模拟结果保留供核对，没有自动修复财务事实。
4. 原子写与锁保证各个账户/T事务的文件一致性，不等同于整个多阶段流程的数据库事务。决策状态、决策历史与最终信号文件之间仍没有全局原子提交；主机异常退出后的跨文件对账仍需核验。
5. 旧的自由文本AI选股入口可继续输出分析，但没有可核验候选及价格契约时不再自动写监测名单。核心生产入口`stock_pool_ai.py`已使用新契约。其他归档研究脚本没有整体重构或参数再校准。

部署前应先备份真实运行目录，在QQ禁发和预案模式下检查一次完整报告，再切换定时任务到这批源码。本次没有替用户切换生产任务。
